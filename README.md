# contructionWeb
To run the project
in /backend
run "npm install" to install packages
then node server.js to start the server

in frontend run

npm install
npm start

or
copy dist folder
npm install http-server
npm run server:prod
